What is Inference: 
    -> Getting an Idea of a variable type based on its value
    
    e.g let data = 10;
        it will automatically detect that data type of data is number because itis initialize with number
        if we try to assign string value then it will give error
        